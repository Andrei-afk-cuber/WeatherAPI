# How to run
## Python
Create .venv: `python -m venv .venv`  
Install dependencies: `pip install -r requirements.txt`  
Migrate: `python manage.py migrate`  
Run server: `python manage.py runserver`
## Docker
Run Docker and execute: `docker-compose up`